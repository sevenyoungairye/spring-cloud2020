server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sevenyoungairye/springcloud-config.git # github url..
          search-paths: springcloud-config # 仓库名称

eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7001/eureka